export interface criarItem {
    codigo_patrimonio: string,
    nome: string,
    descricao: string,
    id_setor: number,
    condicao: string
}

export interface setor {
    setorId: number,
    nome: string,
}